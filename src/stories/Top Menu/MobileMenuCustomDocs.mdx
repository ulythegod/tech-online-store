import { Story } from "@storybook/addon-docs";
import { ArgsTable } from '@storybook/addon-docs';

import MobileMenu from "../../components/MobileMenu/MobileMenu";

# Mobile Menu

MobileMenu is the primary component. Component is availiable only for screens that are less then 1276px.

# Args 
<ArgsTable of={MobileMenu} />

## Default
<Story id="top-menu-mobile-menu--default" />